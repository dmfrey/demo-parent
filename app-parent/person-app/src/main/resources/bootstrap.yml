---
spring:

  config:
    activate:
      on-cloud-platform: kubernetes

  cloud:

    kubernetes:

      config:
        sources:
          - namespace: dfrey-dev
            name: person-app
          - namespace: dfrey-dev-config
            name: person-app-cm
