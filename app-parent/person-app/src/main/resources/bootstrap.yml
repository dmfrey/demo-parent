---
spring:

  config:
    activate:
      on-cloud-platform: kubernetes

  cloud:

    kubernetes:

      config:
        name: person-app
        sources:
          - name: person-app
          - namespace: dfrey-dev-config
            name: person-app-cm
