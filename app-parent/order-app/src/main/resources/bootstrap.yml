---
spring:

  config:
    activate:
      on-cloud-platform: kubernetes

  cloud:

    kubernetes:

      config:
        sources:
          - namespace: dfrey-dev
            name: order-app
          - namespace: dfrey-dev-config
            name: order-app-cm

server:
  port: 8080
