---
spring:

  config:
    activate:
      on-cloud-platform: kubernetes

  cloud:

    kubernetes:

      config:
        name: order-app
        sources:
          - name: order-app
          - namespace: dfrey-dev-config
            name: order-app-cm

server:
  port: 8080
